apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: fraud-postgres
    url: postgres:5432
    user: airflow
    secureJsonData:
      password: airflow
    jsonData:
      database: airflow
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
    editable: true
